import Component from "$store/components/ui/NewsletterModal.tsx";
import type { Props } from "$store/components/ui/NewsletterModal.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
